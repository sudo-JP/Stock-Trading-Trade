#include "ib_wrapper.h"

void TradingApp::tickPrice(TickerId tickerId, TickType field, double price, const TickAttrib &attrib) {
    switch(tickerId) {
        case ib_wrapper::BID_PRICE:
         
            break; 
    }
}

